# 🌱 纳西妲 · 与你相识 —— 项目交接文档

> 交接时间:2026-08-18 · 当前状态:**可运行** · 服务器:`http://127.0.0.1:5179`(后台任务中)
> 本文档是唯一权威的"当前状态"快照,接手的会话/开发者以此为起点,不要依赖零散记忆。

---

## 0. 项目一句话

一个本地网页**虚拟女友**:Live2D 纳西妲(2D 二次元)+ DeepSeek AI 聊天 + Edge 神经网络语音 + 长期记忆/好感度 + **摄像头视觉(情绪识别 + 官方手势识别)**。用户要求:不要 3D、不要原神世界观(人设是"小女友")、点击互动已暂时删除。

## 1. 快速启动

```bash
npm install          # 依赖已装好(three/pixi/face-api/mediapipe 均本地,无需外网运行)
node server.js       # 或双击 启动纳西妲.bat
# 浏览器打开 http://127.0.0.1:5179
```
- 端口在 `config.json` 的 `port`(5179)
- **改了 public/ 下的文件不用重启服务器**(静态文件直读);**改了 server.js / config.json 要重启**
- 浏览器缓存问题:index.html 已带 `?v=` 版本号 + 服务器 `Cache-Control: no-cache`;仍异常先 Ctrl+F5

## 2. 技术栈与架构(当前)

| 层 | 技术 | 文件 |
|---|---|---|
| 服务器 | Node 原生 http | `server.js` |
| AI 对话 | DeepSeek API(本地无 Key 兜底回复) | `server.js` + `config.json` |
| 语音 | Edge TTS(msedge-tts 库,zh-CN-XiaoyiNeural) | `server.js` |
| **Live2D 渲染** | pixi.js v7 UMD + pixi-live2d-display cubism4 + 官方 Cubism 核心(内嵌 wasm) | `public/live2d.js` + `public/vendor/live2d/` + `public/live2d/nahida/` |
| 摄像头·情绪 | face-api.js(tinyFaceDetector + faceExpressionNet,7 情绪) | `public/camera.js` + `public/vendor/face-api/` |
| 摄像头·姿态动作 | MediaPipe PoseLandmarker(点头/摇头/挥手/举手) | `public/camera.js` + `public/vendor/mediapipe/` |
| 摄像头·官方手势 | MediaPipe GestureRecognizer(👍✌️🖐👊☝️🤟👎,官方模型) | `public/camera.js` + `public/vendor/mediapipe/gesture_recognizer.task` |
| 前端壳 | `public/index.html` + `public/style.css` + `public/app.js`(聊天/TTS/音乐/夜间/语音输入) | — |

### 页面脚本加载顺序(index.html)
```
face-api.js → camera.js → live2dcubismcore → pixi.min.js → pixi-live2d-display-cubism4.min.js → live2d.js → app.js(module)
```

### 关键文件职责
- `public/app.js` — 前端主逻辑:聊天、TTS/口型、情绪→Live2D 表情、摄像头开关+情绪/动作/手势回应、音乐、夜间、语音输入、主动互动
- `public/live2d.js` — Live2D 控制器(Live2DModel.from 加载 / model.motion 动作 / model.focus 视线 / 参数驱动嘴型+表情)
- `public/camera.js` — 摄像头:情绪检测 + 姿态动作 + 官方手势,左下角面板,onEmotion/onAction/onGesture 回调
- `data/memory.json` — 长期记忆(消息 + 好感度 affection)

## 3. 核心功能与当前状态

1. **聊天**:DeepSeek 驱动,纳西妲"小女友"人设(温柔体贴、爱看书、会撒娇),禁止原神世界观;回复带【emotion:】情绪标签
2. **关系阶段**:初识→相识→熟悉→亲近,每 10 句升一档;**隐藏好感度**(-100~100,初始15,关键词增减),只影响语气不显示数值
3. **长期记忆**:最近 80 条消息,对话携带最近 24 条
4. **语音**:Edge 晓伊(+14% 语速/+20Hz 音调,低龄感);情绪→语速音调;失败回退浏览器语音;**开始录音前自动静音 TTS**(回声修复)
5. **Live2D**:纳西妲立绘(moc3 v4,83 参数),眨眼由 idle 动作内置曲线(注意:此模型**无内置动作**,是静态立绘,靠参数驱动"活":眨眼/嘴型/表情/视线跟随鼠标)
6. **摄像头(📷 按钮)**:左下角镜像画面+人脸框+情绪标签;她对情绪回应(难过安慰/开心同乐,25s 冷却);对**官方手势**回应(👍✌️🖐👊☝️🤟👎,12s 冷却);对姿态动作回应(点头/摇头/挥手/举手,20s 冷却);**回应语音统一 normal 语调**(保证与聊天声音一致)
7. **点击互动:已删除**(用户要求暂时移除,`/api/interact` 接口保留)
8. **主动互动**:每 2~4 分钟她主动说一句话
9. **氛围**:音乐盒 BGM(WebAudio 合成)、夜晚模式(萤火虫)、绿叶粒子、自定义称呼

## 4. 关键配置(config.json)

```json
{
  "port": 5179,
  "deepseek": { "apiKey": "sk-...(建议用户重置,曾在对话中暴露)", "model": "deepseek-chat" },
  "tts": { "voice": "zh-CN-XiaoyiNeural", "rate": "+14%", "pitch": "+20Hz", "volume": "+0%" },
  "persona": { "name": "纳西妲", "userName": "亲爱的", "systemPrompt": "...小女友人设,禁止游戏世界观,禁止括号神态,输出【emotion:】标签" }
}
```

## 5. 踩坑记录(精华,接手必读)

1. **换人设/换角色后必须清空 `data/memory.json`**——旧人设历史对话会作为 few-shot 把 AI 带偏(换纳西妲/塔菲时都踩过)。备份再清。
2. **换 Live2D 模型**:只改 `public/live2d.js` 里的模型 URL + `_applyEmotion` 的参数名。**每个模型的参数命名风格不同**(明石用 `PARAM_BROW_L_Y`,拉菲/纳西妲用 `ParamBrowLY`),必须从该模型的 motion3.json 动作曲线里重新提取参数名。model3.json 的 Motions 要按 Idle/TapBody/TapHead 分组(`model.motion('Idle')` 按组播放)。
3. **pixi-live2d-display 必须用 `dist/cubism4.min.js` 专用构建**(`dist/index.min.js` 是全量版,加载即要求 Cubism2 核心 live2d.min.js,没有就抛错导致 PIXI.live2d 挂载失败——本项目踩过)。
4. **Cubism 核心版本**:3.3.0 旧核心缺 `Logging`/`Version`,cubism4 构建会在 `csmSetLogFunction` 崩溃;必须用**官方核心**(cubism.live2d.com,内嵌 wasm 单文件,已用)。
5. **服务器崩溃防护**(server.js 顶部):msedge-tts 的 WebSocket 错误会在库内 throw,不加 `process.on('uncaughtException'/'unhandledRejection')` 整个服务器会挂(踩过,已加);TTS 失败时重置 ttsInstance 让下次重建连接。
6. **摄像头动作识别**:MediaPipe PoseLandmarker 用 **tasks-vision 0.10.35**(1.0.1 有问题);GPU delegate 失败要回退 CPU;检测按 `video.currentTime` 变化触发(时间戳必须递增);**动作分类要用"方向翻转状态机"**(摇头=左→右→左完整翻转≥2次,死区过滤噪声),只看偏移量会"始终误报摇头"(踩过,已修,`tools/sim_actions.mjs` 可回归验证)。
7. **官方手势识别**(GestureRecognizer)是最可靠的(官方模型输出),`gesture_recognizer.task` 内部已含手部关键点,不需要额外 hand_landmarker。
8. **大文件下载**:本环境整包下载 storage.googleapis.com 会超时,**用 Range 分块下载**(工具:tools/fetch_gesture.mjs、fetch_task_chunked.mjs)。
9. **网络**:raw.githubusercontent 极不稳定,优先 **jsDelivr**(cdn.jsdelivr.net/gh/...);GitHub API(git trees/contents)次之;再不行 git sparse-checkout。
10. **PowerShell 写文件带 BOM** 会让 Node JSON.parse 挂;写 JSON 用 Node 的 fs.writeFileSync。
11. **验证方法论**:本环境跑不了浏览器,靠 ①Node 沙箱(vm)验证脚本挂载/模型解析(tools/sim_live2d.mjs 等)②数值模拟验证算法(tools/sim_actions.mjs)③离屏渲染(tools/render_png.mjs,3D 时代遗留)④服务器接口实测(node -e fetch)。**渲染/视觉判读(如 modlens)对二次元模型不可靠,以数值为准。**

## 6. 参考过的项目(诚实标注)

- **[CRPRPO/MediaPipe_Diagnostic_Lab](https://github.com/CRPRPO/MediaPipe_Diagnostic_Lab)** — PoseLandmarker 姿态管线:0.10.35 版本、按视频帧检测、GPU/CPU 回退 ✅ 已照做
- **[BaseMax/face-emotion-detection-web-live](https://github.com/BaseMax/face-emotion-detection-web-live)** — face-api.js 摄像头情绪识别 ✅ 已照做
- **[anime-desktop-pet](https://github.com/SherlockYzz/anime-desktop-pet)** — pixi.js v7 + pixi-live2d-display 技术栈、model.motion/focus 用法 ✅ 已照做
- **[Alex-jam-lab/Genshin-Impact-AI](https://github.com/Alex-jam-lab/Genshin-Impact-AI)** — 纳西妲 Live2D 模型来源 ✅ 直接使用
- **[imuncle/live2d](https://github.com/imuncle/live2d)** — 碧蓝航线模型合集(拉菲/明石来源) ✅ 曾用
- **动作分类器(点头/摇头/挥手)**:⚠️ **自己写的,无直接参考**(方向翻转状态机,参考"活体检测动作完整性验证"通用思路)——这是效果一般的原因,接手可考虑换更成熟方案
- N.E.K.O / moeru-ai/three-mmd(3D 时代参考,已废弃)

## 7. 已知问题 / 待办

1. **动作识别(点头/摇头/挥手)仍不够可靠**:依赖半身入镜、阈值敏感;官方手势识别是可靠的替代。可考虑:挥手改用 hand_landmarker 追踪手腕摆动
2. 纳西妲模型是**静态立绘**(无内置动作),"活"感弱于带动作的模型;若拿到带动作的模型(如 B站 配布的完整角色),换模型即可
3. 点击互动已删除,`/api/interact` 接口保留,想恢复只需在 app.js 加回 onLive2DTap 绑定
4. `public/idle_motion.json`、`public/model/nahida/`(3D 模型)、`public/live2d/lafei/` 是**历史遗留未删**(3D 时代/塔菲时代),不影响运行,可清理(约 10MB+)
5. `public/vendor/live2dcubismcore.min.js`(vendor 根目录)是重复文件,真正用的是 `public/vendor/live2d/live2dcubismcore.min.js`
6. **DeepSeek API Key 建议用户重置**(曾在对话中明文暴露)
7. 若用户提供塔菲/其他角色的 `.moc3` 模型,接法见踩坑 #2

## 8. 交接注意点

- 用户核心诉求回顾:①不要 3D ②要二次元感 ③**拒绝原神世界观**、要"小女友"感 ④声音要一致(反馈=聊天)⑤点击互动先删 ⑥摄像头=情绪+手势识别
- 用户对"参考其他项目"很在意——接手后做功能时**真正去读参考项目代码**并如实说明参考了什么、什么没参考
- 改完任何东西都要实测:node --check、接口 fetch、必要时重启服务器

## 9. 快速自检清单

```bash
node --check server.js public/app.js public/live2d.js public/camera.js
curl http://127.0.0.1:5179/api/config   # name=纳西妲, hasKey=true
curl http://127.0.0.1:5179/api/tts -X POST -H 'Content-Type: application/json' -d '{"text":"测试","emotion":"normal"}'  # 200 audio/mpeg
# 页面:Ctrl+F5,应看到纳西妲 Live2D;点📷应能出摄像头面板+情绪/手势标签
```

## 10. 近期变更记录

| 日期 | 变更 |
|---|---|
| 2026-08-26 | **更换背景**:背景图换为**原神须弥雨林观景点 4K 截图**(米游社「须弥（上）地理志/观景点 4K壁纸图片」帖,ID 74598410;3840×2160 原图裁剪右下角小水印后压至 2560×1436 → `public/bg/nahida-forest.jpg`)。画风为原神官方渲染(精致二次元),与 Live2D 立绘同调;上半部开阔蓝天,人物头部位置无遮挡。实现:style.css 中 `#scene-wrap` 多层 background(罩层+图+兜底渐变);梦境光斑 `::before` 改为柔和金白日光微尘;夜晚模式用 `#scene-wrap::after` 深蓝面纱(只压背景不压人物,opacity 平滑过渡) |
