# 项目总结(需求 · 踩坑 · 重要认知)

> 与 PROJECT_STATE.md(技术速查)配合阅读。本文档按"需求→踩坑→认知"三部分完整汇总。

---

## 一、你的需求(按时间线整理)

| # | 需求 | 最终状态 |
|---|------|---------|
| 1 | 会动的胡桃虚拟女友,最好能发声 | ✅ 初始为 3D 胡桃(官方 MMD 模型) |
| 2 | 形态:网页应用;DeepSeek Key;Edge 神经网络音色 | ✅ 网页应用 + 晓伊音色(晓晓→晓伊→低龄高音) |
| 3 | 加分功能全要:长期记忆、语音输入、表情动作响应、氛围彩蛋 | ✅ 全部实现 |
| 4 | 动作不要太僵硬、去人机感 | ✅ 连续噪声流 + 物理/程序化头发(多轮迭代) |
| 5 | 声音要古灵精怪 | ✅ 情绪化语速音调 + 俏皮句夸张处理 |
| 6 | 不要"女友模式",要**模拟从初识到熟悉** | ✅ 关系阶段系统(初识→相识→熟悉→亲近) |
| 7 | 手部自然下垂、视角拉大 | ✅ 手臂矫正(后因绝对坐标 bug 修正) |
| 8 | 视角拉近到"正好看不到腿";聊天框**半透明微信风格** | ✅ 相机 1.375/1.39 构图 + 微信风 UI(绿气泡/毛玻璃) |
| 9 | 背景别单调、面部要有表情会眨眼、手部别只直上直下 | ✅ 极光背景+花瓣+萤火虫;morphTargets 修复后表情正常;手的多轴动作 |
| 10 | **换成纳西妲** | ✅ 官方 5 周年模型 + 全链路人设/台词/UI 切换 |
| 11 | 声音更低龄;**别读括号神态**;参考项目学流畅动作 | ✅ 高音调+括号剥离+参考 N.E.K.O/moeru-ai |
| 12 | 主动互动、微表情、镜头生命感(我补充的"活"要素) | ✅ 主动说话/微表情脉冲/凝视镜头/镜头呼吸 |
| 13 | 点击镜头不要动、头发别乱抖 | ✅ 移除镜头脉冲;关闭物理改程序化头发 |
| 14 | 更精致模型/动作 | ⚠️ 调研结论:真·待机 VMD 和精品模型都在登录墙后(44mmd/Bowlroll),未能获得;当前用官方模型+程序化方案 |
| 15 | 继续参考别人项目 | ✅ 扒了 N.E.K.O(情感双轴)、moeru-ai/three-mmd(管线)源码 |
| 16 | **动作还不够自然流畅,参考同类项目解决** | ✅ 动作系统 v2:值噪声替代正弦、关节链延迟跟随、重心转移、微场景全身化、鼠标跟随;并修复手臂外翻 bug(见下) |
| 17 | **背景不"应景"(还是胡桃紫粉风)** | ✅ 全面换纳西妲草元素主题:绿/金极光、绿叶粒子、绿金柔光,UI 变量 --hutao-* → --nahida-* |
| 18 | **对话原神设定占比太重,要虚拟女友感** | ✅ 人设/主动台词/点击反应/兜底回复全部去原神化;默认称呼 旅行者→亲爱的;旧记忆备份清空 |
| 19 | **动作还是不够自然,质疑没参考别人代码** | ✅ 实际拉取 4 个项目源码 + CMU 动捕数据库:落地注视分层、主控信号、眼动加速 |
| 20 | **待机动作不对,要多参考几个项目** | ✅ **接入真实动捕待机**:CMU subject 140 "Idle" 重定向 → idle_motion.json 驱动 4 根躯干骨 + 程序化叠加 |
| 21 | **手臂几乎不动/太僵硬** | ✅ **手臂也接入动捕**:受试者手臂真实微动(±30-38°)重定向到 腕/ひじ/手首(缩放 0.35/0.3/0.3)→ 手横向摆幅 ±5.7%、前后 ±10% 身高;程序化手臂改为叠加层(微噪声/说话/挥手/场景口音,幅度×0.65) |
| 22 | **人物整体略微机械晃动** | ✅ 三重修复:60fps 烘焙(原 30fps 线性插值步进感)+ 浏览器 smoothstep 插值(C1 连续)+ 3 次低通滤波;**最佳匹配拼接**+反向交叉淡化 → 循环接缝从 1.7° 降到 0.00°,润色段无倒带/跳变 |
| 23 | **要"不需要3D建模"的二次元感 + 换成塔菲 + 别弄3D了** | ✅ **Live2D 2D 二次元模式(唯一渲染,3D 已移除)**:技术栈参考 anime-desktop-pet(pixi.js v7 + pixi-live2d-display);碧蓝航线明石猫娘模型;角色身份是永雏塔菲 |
| 24 | **Live2D 加载失败 → 分析修复** | ✅ 根因有两个:①dist/index.min.js 全量版强制要求 Cubism2 核心 → 换 cubism4.min.js 专用构建;②免疫 3.3.0 核心缺 Logging/Version,cubism4 构建在 csmSetLogFunction 崩溃 → 换官方 Cubism4 核心(内嵌 wasm 单文件,README 指定)。另加核心就绪等待 + 失败重试 + 真实错误上屏;用 check_api.mjs 沙箱对比"库调用的 API vs 核心导出"精确定位 |
| 25 | **模型不像塔菲(绿毛明石)** | ✅ 换**拉菲**(白毛红瞳,开源 Live2D 里最接近塔菲形象);动作组/表情参数(ParamBrowLY 等)按拉菲模型重配;明石已删除。塔菲本体模型无开源渠道——若用户提供她的 .moc3 模型文件夹,复制到 public/live2d/ 并改 live2d.js 一行即可 |
| 26 | **别塔菲了,就纳西妲;声音改回 3D 纳西妲的** | ✅ 人设/台词/UI 全量还原纳西妲小女友(删除塔菲喵/猫娘/关注塔菲喵全部残留);模型用**纳西妲 Live2D**(GitHub 开源桌宠项目 [Genshin-Impact-AI](https://github.com/Alex-jam-lab/Genshin-Impact-AI),moc3 v4 + 83 参数,官方 Cubism4 核心验证可解析,无内置动作但眨眼/嘴型/表情/视线全参数驱动);声音本就是 3D 纳西妲时代的 zh-CN-XiaoyiNeural(+14%/+20Hz),确认未变;猫耳尾巴死代码已删 |
| 27 | **加摄像头视觉:人脸检测+情绪识别,左下角显示** | ✅ **face-api.js 摄像头情绪识别**:📷 开关 → 本地模型检测人脸+7 情绪,左下角镜像画面+人脸框+情绪标签;她会在你难过/生气/开心时主动回应;参考 BaseMax/face-emotion-detection-web-live 实现 |
| 28 | **语音交流录进纳西妲声音 + 加动作识别 + 删点击互动** | ✅ ①**回声修复**:开始录音前暂停/取消 TTS;②**动作识别**:MediaPipe PoseLandmarker(0.10.35,参考 CRPRPO 项目做法:按视频帧检测、GPU→CPU 回退)识别挥手/举手/点头/摇头 → 她回应;③**点击互动已删除** |

## 二、踩坑清单(每条都是血泪)

1. **PMX 骨骼位置是绝对坐标,不是相对父骨骼** —— 早期把位置累加算手臂矫正角度,得出了假的"双臂上举 160°",套上后手消失。修正后确认 A 字自然姿态只需 ±0.5 rad。
2. **three.js 材质必须开 `morphTargets = true`** —— 否则着色器完全无视表情数据,表现为"面部无表情、不眨眼"。修复后 65 个表情全部生效。
3. **Edge TTS 端点限制多**:
   - 不支持 `<break>` 停顿标签
   - 不支持 `<mstts:express-as>` 情感风格
   - 不支持嵌套 prosody;每条请求最多 2 个平级 prosody 元素
   - 负数音量必须写成 `-4%`,写成 `+-4%` 会 500
   → 最终方案:情绪基准语速音调 + 最多两段 prosody + 标点/省略号自然停顿。
4. **Grants(付与骨骼)必须每帧重置为单位旋转** —— 漏掉重置会导致旋转逐帧累乘,模型几十帧内拧成"异形"(纳西妲有 22 个 grant 骨骼:眼睛跟头、手臂扭转)。修复:每帧 `quaternion.set(0,0,0,1)` 再应用 + 递归解父链。
5. **舞蹈 VMD 的"平静窗口"不能当待机** —— 用离屏渲染实测:那些低能量窗口是舞者跳舞中间的姿势(弯腰、蹲坐、手臂交叉),渲染验证后彻底放弃 VMD 待机方案。
6. **换角色必须清空 `data/memory.json`** —— 旧人设(胡桃)的历史对话会作为 few-shot 样本把 AI 带偏,即使系统提示词已是纳西妲,模型仍输出"往生堂本堂主"。
7. **端口/进程检测不可靠** —— `Get-NetTCPConnection` 在沙箱里经常失灵;正确姿势:`netstat -ano | findstr :端口` 拿 PID 再 `Stop-Process`。旧服务器残留会导致新服务器 EADDRINUSE。
8. **后台任务生命周期混乱** —— 多次出现"杀不掉旧进程"和"新任务顶掉旧任务";服务器要作为最后一个后台任务启动。
9. **本环境跑不了 Chrome** —— 沙箱限制导致无法本地浏览器验证;解决办法:写离屏渲染器(蒙皮+光栅化出 PNG)+ 视觉工具(modlens)验证模型/动作/表情。
10. **渲染器自身两个 bug** —— ①骨骼层级根节点不是 0 号骨骼,updateMatrixWorld 要遍历所有根;②VMD 关键帧数组并非按帧排序,取关键帧要找"最大帧号"而非"数组最后一个"。
11. **PowerShell 传参吞空串** —— 空参数 `""` 会被丢弃导致参数错位;脚本参数设计要避免空串占位。
12. **编辑 JSON 时换行未转义** —— 用编辑工具在 JSON 字符串内插入真实换行会损坏 JSON(提示词超长时踩过),改完必须 `JSON.parse` 验证。
13. **TTS 偶发失败** —— 服务器端缓存 tts 实例的变量名写错(存到实例上却从模块上读)导致后续请求 undefined;以及微软服务偶发限流。前台/后台沙箱环境下 WebSocket 行为不一致(前台验证通过即可)。
14. **手臂矫正参数不能跨模型复用(本轮血泪)** —— 胡桃模型调的 ARM_POSE(±0.5) 直接沿用给纳西妲,结果手臂被向外掰成 17.7° 翼状外翻(视觉上"动作不自然"的主因之一)。用骨骼世界坐标数值测量发现:纳西妲静止时上臂本就接近垂直(约 7°)。修复:z=0.05/-0.05 + 肘 0.12 + 腕 0.08。
15. **离屏渲染 + modlens 判读有陷阱** —— 纳西妲宽袖 + 深色模型 + 深灰背景,modlens 会把袖子误读成"手臂张开/交叉/受束缚"。教训:动作/姿态是否自然,以**骨骼世界坐标数值**(肩→肘→腕向量夹角)为准,渲染图只做粗略参考;需要看轮廓时把背景提亮(渲染工具 argv[13])。
16. **PowerShell 写文件带 BOM** —— `Set-Content -Encoding UTF8` 在部分环境写 BOM,Node `JSON.parse` 直接报 "Unexpected token"。写 JSON/数据文件一律用 Node 的 `fs.writeFileSync(..., 'utf8')`。

## 三、重要认知(参考项目学到的 + 项目决策)

### 从参考项目学到的
- **[N.E.K.O 猫娘](https://github.com/Project-N-E-K-O/N.E.K.O)**:用 **valence(愉悦)+ arousal(激活)双轴连续情感** 驱动表情,比离散情绪跳变自然得多;角色会**主动找你说话**。"活"的本质是"她会自己动、自己有情绪变化"。
- **[moeru-ai/three-mmd](https://github.com/moeru-ai/three-mmd)**:专业 MMD 每帧管线 = **保存姿势 → updateMatrixWorld → IK 解算 → Grants 附加变换 → 物理**,顺序不能乱;骨骼处理要防重复应用(保存/恢复模式)。
- **Live2D 系项目**(anime-desktop-pet 等):相机基本固定、无物理抖动,靠**手调循环动画**+表情。→ 支撑了"相机稳定 + 关闭物理"的决策。
- **VRM 桌宠/虚拟女友类项目(本轮)**:有机待机的三个关键做法 —— ①**多倍频平滑值噪声**替代正弦叠加(非周期、有机漂移);②**关节链延迟跟随**(肩→肘→腕同一噪声流时间错位,鞭梢式,杜绝"三根独立棍子各晃各的");③**重心转移**(骨盆椭圆 + 躯干反向补偿 + 头部保持,经典 S 曲线)。+ 鼠标跟随头部/眼睛。→ 全部落地。
- **pmx-pal / airi 源码精读(本轮第二轮)**:真正拉开源码后学到三个此前缺失的细节 —— ①**注视必须是"叠加层"而不是"覆盖"**:颈/头/眼按 0.12/0.32/0.56 权重分配、±35°/±20° 限幅、`1-exp(-k·dt)` 指数平滑,乘在基础动作之上;②**自然的根源是整体连贯**:真人待机 = 一个呼吸/摇摆主信号派生全身,而不是每根骨骼独立噪声(v3 已重构为主信号派生);③**眼动扫视比想象中快**:真人 0.8~2.4s 一次(v3 已加速)。
- **结论**:可下载的真·待机 VMD 和更精致模型都在登录墙后(Bowlroll/44mmd/Bilibili),开源 VMD 合集(bear0830/mmd)只有舞蹈。当前最优可行方案 = 官方模型 + 模拟真实待机结构的程序化动画(v3)+ IK + Grants + 表情/互动系统;若将来拿到真待机 VMD,按 airi 的 AnimationMixer + 0.4s crossfade 方案接入即可。

### 关键决策与认知
- **动作流畅 = 连续噪声流(flow)**:用黄金比例频率(1.618/0.618)的多层正弦叠加,永不重复、无突变,取代"目标跳变+弹簧追赶"(后者有人机感)。
- **物理引擎(349 刚体)默认关闭**:浏览器变帧率下刚体模拟抖动明显;改用程序化摆动覆盖 79 根头发/披风/树叶骨骼,顺滑零抖动。代码保留,`ENABLE_PHYSICS` 开关可随时开启。
- **验证方法论**:改模型/动作/表情后,先用 `tools/render_png.mjs` 离屏渲染出图,再用视觉工具(modlens)检查,不要盲改盲交。
- **API Key 安全**:你的 DeepSeek Key 曾在对话中明文出现,建议到 DeepSeek 后台重置一次;它只存在本机 `config.json`。
- **数据都在本地**:代码/模型/记忆(含好感度)全在磁盘,换会话或重启电脑后项目状态不丢。

## 四、当前状态与待办
- ✅ 运行中:`http://127.0.0.1:5179`(启动:`启动纳西妲.bat` / `node server.js`)
- ✅ 本轮完成:动捕待机升级——**手臂也接入真实动捕**(10 根骨:躯干 4 + 手臂 6),修复"手臂太僵硬";另有注视分层、眼动加速、背景草元素主题化、人设去原神化、旧记忆备份清空
- 待办:物理引擎再调优后可选开启;更精致模型/真待机 VMD(需登录墙资源);DeepSeek Key 重置建议
- 技术细节速查见 `PROJECT_STATE.md`
